# Styling

## Best Practices

1. **Tailwind CSS** - Use utility classes, avoid inline styles
2. **Responsive design** - Use Tailwind breakpoints (`sm:`, `md:`, etc.)
3. **Dark mode** - Use `dark:` prefix for dark mode styles
4. **Class organization** - Use `clsx` for conditional classes
