# TypeScript

## Best Practices

1. **Type definitions** - Keep types in `types/` directories
2. **Type imports** - Use `import type` for type-only imports
3. **Strict typing** - Avoid `any`, use proper types or `unknown`
4. **Const assertions** - Use `as const` for constant objects
